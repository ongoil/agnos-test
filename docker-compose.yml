services:
  api:
    build: .
    container_name: agnos-api
    restart: unless-stopped
    environment:
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_USER: agnosdb
      DB_PASSWORD: agnos1234
      DB_NAME: agnos-test
      DB_SSLMODE: disable
      JWT_SECRET: agnos-test-secret
      JWT_EXPIRE_HOURS: "24"
      APP_PORT: "5000"
    depends_on:
      postgres:
        condition: service_healthy
    expose:
      - "5000"

  postgres:
    image: postgres:16
    container_name: agnos-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: agnos-test
      POSTGRES_USER: agnosdb
      POSTGRES_PASSWORD: agnos1234
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agnosdb -d agnos-test"]
      interval: 5s
      timeout: 5s
      retries: 10

  nginx:
    image: nginx:alpine
    container_name: agnos-nginx
    restart: unless-stopped
    depends_on:
      - api
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro

volumes:
  postgres_data:
